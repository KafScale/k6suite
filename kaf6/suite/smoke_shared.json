{
  "name": "smoke_shared",
  "description": "S3 shared-style loop with single client",
  "profile": "local-service",
  "brokers": ["127.0.0.1:39092"],
  "topics": [
    {
      "name": "smoke-shared-{{run_id}}",
      "partitions": 1,
      "recreate": true
    }
  ],
  "scenarios": {
    "producer": {
      "type": "produce",
      "clients": 1,
      "messages": 200,
      "rate_per_s": 0,
      "topic": "smoke-shared-{{run_id}}",
      "value": {
        "json": {
          "uuid": "{{uuid}}",
          "ts": "{{now}}"
        }
      }
    },
    "consumer": {
      "type": "consume",
      "clients": 1,
      "group": {
        "id": "smoke-shared-{{run_id}}"
      },
      "topic": "smoke-shared-{{run_id}}",
      "offset": "earliest",
      "limit": 200,
      "timeout": "30s"
    }
  },
  "checks": [
    {
      "name": "delivered_all",
      "type": "count_equals",
      "expected": 200
    }
  ]
}
